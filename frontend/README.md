# Ascooo iQIYI Logo Finder

```powershell
npm install
Copy-Item .env.example .env
npm run dev
```

The app defaults to `http://localhost:8000`. Set `VITE_API_BASE_URL` for Cloud Run before building for Cloudflare Pages.
